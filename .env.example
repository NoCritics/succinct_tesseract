# Backend configuration
PORT=3002
NODE_ENV=production

# Frontend API endpoint (if different from default)
VITE_API_ENDPOINT=https://yourdomain.com/api/explorer/latest-proof

# Scraper settings
CACHE_DURATION=30000
HEADLESS_BROWSER=true